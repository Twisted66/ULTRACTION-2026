---
import Layout from '../layouts/Layout.astro';

const title = 'Careers Admin - ULTRACTION';
const description =
  'Internal HR panel for manually publishing open roles to the careers feed.';
const jobsApiBaseUrl = import.meta.env.PUBLIC_JOBS_API_BASE_URL?.trim() ?? '';
---

<<<<<<< HEAD
<Layout title={title} description={description} image="/logo/UGC-HEADER.png">
=======
<Layout title={title} description={description} image="/logo/UGC-HEADER.png" noIndex={true}>
>>>>>>> origin/master
  <section class="border-t border-border bg-background py-20">
    <div class="container mx-auto px-6 lg:px-10 max-w-4xl">
      <div class="mb-10">
        <p class="text-xs uppercase tracking-[0.28em] text-accent font-bold mb-4">Internal HR</p>
        <h1 class="text-4xl lg:text-6xl font-heading font-bold uppercase leading-none mb-4">
          Careers Admin
        </h1>
        <p class="text-sm opacity-70 max-w-2xl">
          Use this page to manually publish open career opportunities. Keep this route private and
          only share with authorized HR staff.
        </p>
<<<<<<< HEAD
=======
        <p class="text-xs opacity-65 max-w-2xl mt-3">
          This panel and ChatGPT Actions use the same backend endpoint: <code>/api/jobs</code>.
          Create, update, and archive roles from either interface.
        </p>
>>>>>>> origin/master
      </div>

      <div class="border border-primary/15 bg-surface/70 p-8 mb-8">
        <form id="create-job-form" class="space-y-6" autocomplete="off">
          <div class="space-y-2">
            <label class="text-xs uppercase tracking-[0.2em] font-bold" for="jobs-api-key"
              >Jobs API Key</label
            >
            <input
              id="jobs-api-key"
              name="jobsApiKey"
              type="password"
              required
              class="w-full bg-background border border-primary/20 px-4 py-3 text-sm"
              placeholder="Paste server-side JOBS_API_KEY"
            />
            <p class="text-xs opacity-60">
              Never commit this key. It stays in your browser session only.
            </p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="space-y-2">
              <label class="text-xs uppercase tracking-[0.2em] font-bold" for="title">Role Title</label>
              <input
                id="title"
                name="title"
                type="text"
                required
                minlength="2"
                maxlength="120"
                class="w-full bg-background border border-primary/20 px-4 py-3 text-sm"
                placeholder="Site Engineer"
              />
            </div>
            <div class="space-y-2">
              <label class="text-xs uppercase tracking-[0.2em] font-bold" for="location">Location</label>
              <input
                id="location"
                name="location"
                type="text"
                required
                minlength="2"
                maxlength="120"
                class="w-full bg-background border border-primary/20 px-4 py-3 text-sm"
                placeholder="Abu Dhabi, UAE"
              />
            </div>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="space-y-2">
              <label class="text-xs uppercase tracking-[0.2em] font-bold" for="department"
                >Department (Optional)</label
              >
              <input
                id="department"
                name="department"
                type="text"
                maxlength="120"
                class="w-full bg-background border border-primary/20 px-4 py-3 text-sm"
                placeholder="Operations"
              />
            </div>
            <div class="space-y-2">
              <label class="text-xs uppercase tracking-[0.2em] font-bold" for="employmentType"
                >Employment Type (Optional)</label
              >
              <input
                id="employmentType"
                name="employmentType"
                type="text"
                maxlength="80"
                class="w-full bg-background border border-primary/20 px-4 py-3 text-sm"
                placeholder="full-time"
              />
            </div>
          </div>

          <div class="space-y-2">
            <label class="text-xs uppercase tracking-[0.2em] font-bold" for="postedAt"
              >Posted Date (Optional)</label
            >
            <input
              id="postedAt"
              name="postedAt"
              type="datetime-local"
              class="w-full md:w-72 bg-background border border-primary/20 px-4 py-3 text-sm"
            />
          </div>

          <div class="space-y-2">
            <label class="text-xs uppercase tracking-[0.2em] font-bold" for="description">Description</label>
            <textarea
              id="description"
              name="description"
              rows={6}
              required
              minlength="20"
              maxlength="5000"
              class="w-full bg-background border border-primary/20 px-4 py-3 text-sm resize-y"
              placeholder="Describe responsibilities, required experience, and qualifications."
            ></textarea>
          </div>

          <div class="flex flex-wrap items-center gap-4">
            <button
              id="create-job-submit"
              type="submit"
              class="bg-[#800000] text-white px-7 py-3 text-xs uppercase tracking-[0.2em] font-bold"
            >
              Publish Role
            </button>
            <button
              id="refresh-jobs"
              type="button"
              class="border border-primary/25 px-7 py-3 text-xs uppercase tracking-[0.2em] font-bold"
            >
              Refresh Open Roles
            </button>
          </div>
        </form>
      </div>

      <div class="border border-primary/15 bg-surface/70 p-8">
        <div class="flex flex-wrap items-center justify-between gap-3 mb-4">
          <h2 class="text-sm uppercase tracking-[0.2em] font-bold">Status</h2>
          <span id="open-roles-count" class="text-[10px] uppercase tracking-[0.2em] opacity-60">-</span>
        </div>
        <p id="admin-status" class="text-sm opacity-70">Ready.</p>
        <ul id="open-roles-list" class="mt-6 space-y-3"></ul>
      </div>
    </div>
  </section>
</Layout>

<script is:inline define:vars={{ jobsApiBaseUrl }}>
  const form = document.getElementById('create-job-form');
  const apiKeyInput = document.getElementById('jobs-api-key');
  const postedAtInput = document.getElementById('postedAt');
  const statusElement = document.getElementById('admin-status');
  const listElement = document.getElementById('open-roles-list');
  const rolesCountElement = document.getElementById('open-roles-count');
  const submitButton = document.getElementById('create-job-submit');
  const refreshButton = document.getElementById('refresh-jobs');

  const base = typeof jobsApiBaseUrl === 'string' ? jobsApiBaseUrl.trim().replace(/\/$/, '') : '';
  const endpoint = base ? `${base}/api/jobs` : '/api/jobs';
  const API_KEY_STORAGE_KEY = 'ultraction-jobs-api-key';

  function setStatus(message, isError = false) {
<<<<<<< HEAD
    if (!statusElement) return;
=======
    if (!(statusElement instanceof HTMLElement)) return;
>>>>>>> origin/master
    statusElement.textContent = message;
    statusElement.classList.toggle('text-red-600', isError);
    statusElement.classList.toggle('text-emerald-700', !isError);
  }

  function toIsoFromLocalDateTime(value) {
    if (!value) return undefined;
    const date = new Date(value);
    return Number.isNaN(date.getTime()) ? undefined : date.toISOString();
  }

  function escapeHtml(value) {
    return String(value)
      .replaceAll('&', '&amp;')
      .replaceAll('<', '&lt;')
      .replaceAll('>', '&gt;')
      .replaceAll('"', '&quot;')
      .replaceAll("'", '&#39;');
  }

<<<<<<< HEAD
  function renderRoles(jobs) {
    if (!rolesCountElement || !listElement) return;
=======
  function getJobsApiKey() {
    if (!(apiKeyInput instanceof HTMLInputElement)) {
      return '';
    }
    return apiKeyInput.value.trim();
  }

  function rememberJobsApiKey(apiKey) {
    if (apiKey) {
      sessionStorage.setItem(API_KEY_STORAGE_KEY, apiKey);
    }
  }

  async function sendRequest(url, options = {}) {
    const response = await fetch(url, {
      cache: 'no-store',
      ...options,
      headers: {
        Accept: 'application/json',
        ...(options.headers ?? {}),
      },
    });

    const payload = await response.json().catch(() => ({}));
    if (!response.ok) {
      const message = payload?.error?.message ?? payload?.message ?? `Request failed (${response.status})`;
      throw new Error(message);
    }

    return payload;
  }

  function renderRoles(jobs) {
    if (!(rolesCountElement instanceof HTMLElement) || !(listElement instanceof HTMLElement)) return;
>>>>>>> origin/master
    if (!Array.isArray(jobs) || jobs.length === 0) {
      rolesCountElement.textContent = '0 Open Roles';
      listElement.innerHTML = '<li class="text-sm opacity-60">No open roles yet.</li>';
      return;
    }

    rolesCountElement.textContent = `${jobs.length} Open Role${jobs.length > 1 ? 's' : ''}`;
    listElement.innerHTML = jobs
      .map((job) => {
<<<<<<< HEAD
        const title = escapeHtml(job?.title ?? 'Untitled Role');
        const location = escapeHtml(job?.location ?? 'UAE');
        const department = escapeHtml(job?.department ?? '');
        const postedAt = job?.postedAt ? new Date(job.postedAt).toLocaleDateString() : '-';

        return `
          <li class="border border-primary/15 px-4 py-4">
            <p class="text-sm font-bold uppercase leading-tight">${title}</p>
            <p class="text-xs uppercase tracking-widest opacity-65 mt-2">${location}${department ? ` - ${department}` : ''}</p>
            <p class="text-[11px] opacity-60 mt-2">Posted: ${postedAt}</p>
            <p class="text-[11px] opacity-50 mt-1">Slug: ${escapeHtml(job?.slug ?? '-')}</p>
            <div class="mt-3">
              <button
                type="button"
                data-archive-job="true"
                data-job-slug="${escapeHtml(job?.slug ?? '')}"
                data-job-title="${title}"
                class="border border-red-600/40 text-red-700 px-3 py-2 text-[10px] uppercase tracking-[0.2em] font-bold"
              >
                Archive Role
              </button>
=======
        const title = typeof job?.title === 'string' ? job.title : 'Untitled Role';
        const location = typeof job?.location === 'string' ? job.location : 'UAE';
        const department = typeof job?.department === 'string' ? job.department : '';
        const postedAt = job?.postedAt ? new Date(job.postedAt).toLocaleDateString() : '-';
        const jobId = typeof job?.id === 'string' ? job.id : '';
        const safeTitle = escapeHtml(title);
        const safeLocation = escapeHtml(location);
        const safeDepartment = escapeHtml(department);
        const safePostedAt = escapeHtml(postedAt);
        const safeJobId = escapeHtml(jobId);
        const applyLink = `/contact?subject=careers&role=${encodeURIComponent(title)}`;

        return `
          <li class="border border-primary/15 px-4 py-4">
            <p class="text-sm font-bold uppercase leading-tight">${safeTitle}</p>
            <p class="text-xs uppercase tracking-widest opacity-65 mt-2">${safeLocation}${safeDepartment ? ` • ${safeDepartment}` : ''}</p>
            <p class="text-[11px] opacity-60 mt-2">Posted: ${safePostedAt}</p>
            <div class="mt-4 flex flex-wrap items-center gap-2">
              <a
                href="${applyLink}"
                class="border border-primary/25 px-3 py-2 text-[10px] uppercase tracking-[0.16em] font-bold hover:border-accent/70 transition-colors"
              >
                Apply Link
              </a>
              ${
                safeJobId
                  ? `<button
                      type="button"
                      data-action="archive"
                      data-job-id="${safeJobId}"
                      data-job-title="${safeTitle}"
                      class="border border-red-800/40 text-red-700 px-3 py-2 text-[10px] uppercase tracking-[0.16em] font-bold hover:bg-red-50 transition-colors"
                    >
                      Archive / Remove
                    </button>`
                  : ''
              }
>>>>>>> origin/master
            </div>
          </li>
        `;
      })
      .join('');
  }

  async function loadOpenRoles() {
    try {
<<<<<<< HEAD
      const response = await fetch(endpoint, {
        method: 'GET',
        headers: { Accept: 'application/json' },
        cache: 'no-store',
      });
      const payload = await response.json();
      const jobs = payload?.data?.jobs ?? payload?.jobs ?? [];
      renderRoles(jobs);
    } catch (error) {
      console.error('Unable to refresh open roles on /careers-admin:', error);
    }
  }

  if (apiKeyInput) {
=======
      const payload = await sendRequest(endpoint, { method: 'GET' });
      const jobs = payload?.data?.jobs ?? payload?.jobs ?? [];
      if (!Array.isArray(jobs)) {
        throw new Error('Jobs API returned an unexpected payload.');
      }
      renderRoles(jobs);
    } catch (error) {
      console.error('Unable to refresh open roles on /careers-admin:', error);
      if (rolesCountElement instanceof HTMLElement) {
        rolesCountElement.textContent = '-';
      }
      if (listElement instanceof HTMLElement) {
        listElement.innerHTML = '<li class="text-sm text-red-700">Unable to load roles from backend API.</li>';
      }
      throw error;
    }
  }

  if (apiKeyInput instanceof HTMLInputElement) {
>>>>>>> origin/master
    const savedApiKey = sessionStorage.getItem(API_KEY_STORAGE_KEY);
    if (savedApiKey) {
      apiKeyInput.value = savedApiKey;
    }
  }

  form?.addEventListener('submit', async (event) => {
    event.preventDefault();
<<<<<<< HEAD
=======
    if (!(form instanceof HTMLFormElement)) return;

>>>>>>> origin/master
    const formData = new FormData(form);
    const apiKey = String(formData.get('jobsApiKey') ?? '').trim();
    const title = String(formData.get('title') ?? '').trim();
    const location = String(formData.get('location') ?? '').trim();
    const description = String(formData.get('description') ?? '').trim();
    const department = String(formData.get('department') ?? '').trim();
    const employmentType = String(formData.get('employmentType') ?? '').trim();
    const postedAt = toIsoFromLocalDateTime(String(formData.get('postedAt') ?? '').trim());

    if (!apiKey) {
      setStatus('Missing Jobs API key. Ask engineering for JOBS_API_KEY.', true);
      return;
    }

    const body = { title, location, description };
    if (department) body.department = department;
    if (employmentType) body.employmentType = employmentType;
    if (postedAt) body.postedAt = postedAt;

    try {
<<<<<<< HEAD
      if (submitButton) submitButton.setAttribute('disabled', 'true');
      setStatus('Publishing role...');

      const response = await fetch(endpoint, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Accept: 'application/json',
=======
      if (submitButton instanceof HTMLButtonElement) {
        submitButton.setAttribute('disabled', 'true');
      }

      setStatus('Publishing role...');
      const payload = await sendRequest(endpoint, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
>>>>>>> origin/master
          Authorization: `Bearer ${apiKey}`,
        },
        body: JSON.stringify(body),
      });

<<<<<<< HEAD
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        const message = payload?.error?.message ?? `Request failed (${response.status})`;
        throw new Error(message);
      }

      sessionStorage.setItem(API_KEY_STORAGE_KEY, apiKey);
      form.reset();
      if (apiKeyInput) apiKeyInput.value = apiKey;
      if (postedAtInput) postedAtInput.value = '';
=======
      rememberJobsApiKey(apiKey);
      form.reset();
      if (apiKeyInput instanceof HTMLInputElement) apiKeyInput.value = apiKey;
      if (postedAtInput instanceof HTMLInputElement) postedAtInput.value = '';

>>>>>>> origin/master
      const createdTitle = payload?.data?.job?.title ?? title;
      setStatus(`Published successfully: ${createdTitle}`);
      await loadOpenRoles();
    } catch (error) {
      console.error('Unable to create job from /careers-admin:', error);
      const message = error instanceof Error ? error.message : 'Unable to publish role.';
      setStatus(message, true);
    } finally {
<<<<<<< HEAD
      if (submitButton) submitButton.removeAttribute('disabled');
=======
      if (submitButton instanceof HTMLButtonElement) {
        submitButton.removeAttribute('disabled');
      }
>>>>>>> origin/master
    }
  });

  refreshButton?.addEventListener('click', async () => {
<<<<<<< HEAD
    setStatus('Refreshing open roles...');
    await loadOpenRoles();
    setStatus('Open roles refreshed.');
=======
    try {
      setStatus('Refreshing open roles...');
      await loadOpenRoles();
      setStatus('Open roles refreshed.');
    } catch (error) {
      const message = error instanceof Error ? error.message : 'Unable to refresh open roles.';
      setStatus(message, true);
    }
>>>>>>> origin/master
  });

  listElement?.addEventListener('click', async (event) => {
    const target = event.target;
<<<<<<< HEAD
    if (!(target instanceof HTMLElement)) return;

    const button = target.closest('[data-archive-job="true"]');
    if (!(button instanceof HTMLButtonElement)) return;

    const apiKey = (apiKeyInput instanceof HTMLInputElement ? apiKeyInput.value : '').trim();
    if (!apiKey) {
      setStatus('Missing Jobs API key. Add key first, then archive.', true);
      return;
    }

    const slug = (button.dataset.jobSlug ?? '').trim();
    const title = (button.dataset.jobTitle ?? '').trim();
    if (!slug) {
      setStatus('Missing job slug. Refresh roles and try again.', true);
      return;
    }

    const approved = window.confirm(`Archive role: ${title || slug}?`);
    if (!approved) return;

    const previousText = button.textContent;
    try {
      button.disabled = true;
      button.textContent = 'Archiving...';
      setStatus(`Archiving ${title || slug}...`);

      const response = await fetch(`${endpoint}?slug=${encodeURIComponent(slug)}&confirm=true`, {
        method: 'DELETE',
        headers: {
          Accept: 'application/json',
=======
    if (!(target instanceof Element)) return;

    const button = target.closest('[data-action="archive"]');
    if (!(button instanceof HTMLButtonElement)) return;

    const jobId = button.dataset.jobId?.trim() ?? '';
    const jobTitle = button.dataset.jobTitle?.trim() ?? '';
    const apiKey = getJobsApiKey();

    if (!jobId || !jobTitle) {
      setStatus('Archive failed: missing role ID or title.', true);
      return;
    }

    if (!apiKey) {
      setStatus('Missing Jobs API key. Provide JOBS_API_KEY to archive roles.', true);
      return;
    }

    const confirmation = window.prompt(
      `Type the exact role title to archive:\n${jobTitle}`,
      '',
    );

    if (confirmation === null) {
      return;
    }

    if (confirmation.trim() !== jobTitle) {
      setStatus('Archive cancelled. Title did not match exactly.', true);
      return;
    }

    try {
      button.setAttribute('disabled', 'true');
      setStatus(`Archiving role: ${jobTitle}...`);

      const archiveUrl = `${endpoint}?id=${encodeURIComponent(jobId)}&confirm=true&confirmTitle=${encodeURIComponent(
        confirmation.trim(),
      )}`;

      await sendRequest(archiveUrl, {
        method: 'DELETE',
        headers: {
>>>>>>> origin/master
          Authorization: `Bearer ${apiKey}`,
        },
      });

<<<<<<< HEAD
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) {
        const message = payload?.error?.message ?? `Request failed (${response.status})`;
        throw new Error(message);
      }

      setStatus(`Archived successfully: ${title || slug}`);
=======
      rememberJobsApiKey(apiKey);
      setStatus(`Archived role: ${jobTitle}`);
>>>>>>> origin/master
      await loadOpenRoles();
    } catch (error) {
      console.error('Unable to archive job from /careers-admin:', error);
      const message = error instanceof Error ? error.message : 'Unable to archive role.';
      setStatus(message, true);
    } finally {
<<<<<<< HEAD
      button.disabled = false;
      button.textContent = previousText ?? 'Archive Role';
    }
  });

  loadOpenRoles();
=======
      button.removeAttribute('disabled');
    }
  });

  loadOpenRoles().then(
    () => setStatus('Ready.'),
    (error) => {
      const message = error instanceof Error ? error.message : 'Unable to load roles.';
      setStatus(message, true);
    },
  );
>>>>>>> origin/master
</script>
