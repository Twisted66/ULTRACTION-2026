---
// Projects page - ULTRACTION General Contracting LLC
// Following Nova Construct prototype exactly

import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import SectionBackground from '../components/common/SectionBackground';

// SEO Metadata
const title = "Projects - ULTRACTION GENERAL CONTRACTING LLC";
const description = "Explore our portfolio of completed construction projects including residential, commercial, industrial, infrastructure, and renovation developments across the UAE.";

const allProjects = await getCollection('projects');
const projectEntries = allProjects
  .map((project) => ({
    slug: project.slug,
    title: project.data.title
  }))
  .sort((a, b) => a.title.localeCompare(b.title));

const projectChunkSize = Math.max(1, Math.ceil(projectEntries.length / 4));
const projectCarouselPages = Array.from({ length: 4 }, (_, pageIndex) =>
  projectEntries.slice(pageIndex * projectChunkSize, (pageIndex + 1) * projectChunkSize)
).filter((page) => page.length > 0);
---

<Layout
  title={title}
  description={description}
  image="/logo/UGC-HEADER.png"
>
  <!-- Hero Section - 2 Column Layout (Matching About/Services) -->
  <!-- Hero Section - Full Width Video Background -->
  <!-- Hero Section - 1:1 Split Layout -->
  <section class="grid grid-cols-1 lg:grid-cols-2 border-t border-b border-black dark:border-black min-h-[75vh] lg:min-h-[85vh]">
    <!-- Left Column: Text -->
    <div class="flex flex-col justify-center px-6 py-16 md:px-12 lg:p-24 border-r border-black dark:border-black relative bg-surface h-full">
      <span class="text-xs uppercase tracking-[0.2em] text-black/60 mb-6 block">Strategic Portfolio</span>
      <h1 class="text-4xl sm:text-5xl lg:text-7xl font-light font-heading leading-tight mb-8">
        Precision in <br/> <span class="italic font-bold text-black">Execution</span>
      </h1>
      <div class="w-12 h-[1px] bg-black/20 mb-8"></div>
      <p class="text-base font-light leading-loose opacity-80 max-w-sm text-black">
        Explore a distinguished record of structural excellence across the UAE. From complex infrastructure and industrial facilities to precision-engineered residential developments, our portfolio reflects a legacy of transformative growth and unwavering quality.
      </p>
    </div>

    <!-- Right Column: HD Standalone Image (100% Occupancy) -->
    <div class="relative overflow-hidden h-[320px] sm:h-[420px] lg:h-full bg-surface">
      <img 
        src="/images/projects-hero-v2.png" 
        alt="Naseem Al Bar Bridge construction project" 
        class="absolute inset-0 w-full h-full object-cover"
        loading="eager"
      />
      {/* Sophisticated brand overlays */}
      <div class="absolute inset-0 pointer-events-none bg-gradient-to-r from-surface via-surface/40 to-transparent z-10"></div>
      <div class="absolute inset-0 pointer-events-none bg-black/5 mix-blend-multiply z-10"></div>
      <div class="absolute inset-0 pointer-events-none border-l border-black/5 z-20"></div>
    </div>
  </section>

  <!-- Filters Section - Full Width -->
  <section class="border-b border-black">
    <div class="grid grid-cols-4 md:grid-cols-8" role="group" aria-label="Filter projects by category">
      <!-- ALL PROJECTS - Active -->
      <button
        type="button"
        data-filter="all"
        aria-pressed="true"
        aria-controls="projects-grid"
        class="filter-btn active h-24 border-r border-b md:border-b-0 border-black dark:border-black flex flex-col items-center justify-center gap-1 transition-all motion-base bg-primary text-white"
      >
        <span class="material-symbols-outlined text-xl">apps</span>
        <span class="text-[10px] font-bold uppercase tracking-wider">ALL</span>
      </button>

      <!-- INFRASTRUCTURE - Inactive -->
      <button
        type="button"
        data-filter="infrastructure"
        aria-pressed="false"
        aria-controls="projects-grid"
        class="filter-btn h-24 border-r border-b md:border-b-0 border-black dark:border-black flex flex-col items-center justify-center gap-1 transition-all motion-base bg-background dark:bg-card text-black"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1v5m-4 0h4"/>
        </svg>
        <span class="text-[10px] font-bold uppercase tracking-wider">INFRA</span>
      </button>

      <!-- MARINE - Inactive -->
      <button
        type="button"
        data-filter="marine"
        aria-pressed="false"
        aria-controls="projects-grid"
        class="filter-btn h-24 border-r border-b md:border-b-0 border-black dark:border-black flex flex-col items-center justify-center gap-1 transition-all motion-base bg-background dark:bg-card text-black"
      >
        <span class="material-symbols-outlined text-xl">sailing</span>
        <span class="text-[10px] font-bold uppercase tracking-wider">MARINE</span>
      </button>

      <!-- HERITAGE - Inactive -->
      <button
        type="button"
        data-filter="heritage"
        aria-pressed="false"
        aria-controls="projects-grid"
        class="filter-btn h-24 border-r border-b md:border-b-0 border-black dark:border-black flex flex-col items-center justify-center gap-1 transition-all motion-base bg-background dark:bg-card text-black"
      >
        <span class="material-symbols-outlined text-xl">account_balance</span>
        <span class="text-[10px] font-bold uppercase tracking-wider">HERITAGE</span>
      </button>

      <!-- OIL & GAS - Inactive -->
      <button
        type="button"
        data-filter="oil-gas"
        aria-pressed="false"
        aria-controls="projects-grid"
        class="filter-btn h-24 border-r border-black dark:border-black flex flex-col items-center justify-center gap-1 transition-all motion-base bg-background dark:bg-card text-black"
      >
        <span class="material-symbols-outlined text-xl">oil_barrel</span>
        <span class="text-[10px] font-bold uppercase tracking-wider">OIL & GAS</span>
      </button>

      <!-- INDUSTRIAL - Inactive -->
      <button
        type="button"
        data-filter="industrial"
        aria-pressed="false"
        aria-controls="projects-grid"
        class="filter-btn h-24 border-r border-black dark:border-black flex flex-col items-center justify-center gap-1 transition-all motion-base bg-background dark:bg-card text-black"
      >
        <span class="material-symbols-outlined text-xl">factory</span>
        <span class="text-[10px] font-bold uppercase tracking-wider">INDUSTRIAL</span>
      </button>

      <!-- RESIDENTIAL - Inactive -->
      <button
        type="button"
        data-filter="residential"
        aria-pressed="false"
        aria-controls="projects-grid"
        class="filter-btn h-24 border-r border-black dark:border-black flex flex-col items-center justify-center gap-1 transition-all motion-base bg-background dark:bg-card text-black"
      >
        <span class="material-symbols-outlined text-xl">home_work</span>
        <span class="text-[10px] font-bold uppercase tracking-wider">RESIDENTIAL</span>
      </button>

      <!-- COMMERCIAL - Inactive -->
      <button
        type="button"
        data-filter="commercial"
        aria-pressed="false"
        aria-controls="projects-grid"
        class="filter-btn h-24 flex flex-col items-center justify-center gap-1 transition-all motion-base bg-background dark:bg-card text-black"
      >
        <span class="material-symbols-outlined text-xl">domain</span>
        <span class="text-[10px] font-bold uppercase tracking-wider">COMMERCIAL</span>
      </button>
    </div>
    <p id="projects-filter-status" class="sr-only" aria-live="polite">Showing all projects.</p>
  </section>

  <!-- Projects Grid - 2:1:2 Masonry Layout -->
  <section class="border-b border-black dark:border-black bg-background" aria-labelledby="projects-grid-heading">
    <h2 id="projects-grid-heading" class="sr-only">Featured Projects</h2>
    <div class="grid grid-cols-1 md:grid-cols-12 auto-rows-min" id="projects-grid">

      <!-- Column 1 -->
      <div class="col-span-12 md:col-span-4 flex flex-col border-r border-black" data-category="infrastructure all">
        <div class="project-card relative group overflow-hidden h-[350px] md:h-[450px] border-b border-black">
          <img alt="Al Tallah Road Bridge Construction, Ajman" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/al-tallah-bridge/photo_010.jpeg" />
          <div class="absolute inset-0 bg-black/10 group-hover:bg-black/30 transition-colors motion-base"></div>
          <div class="absolute bottom-4 left-4 right-4 z-20">
            <div class="p-4">
              <span class="text-[10px] uppercase tracking-widest block mb-1 text-white drop-shadow-md">Bridge Construction</span>
              <h3 class="text-lg font-bold uppercase leading-tight text-white drop-shadow-md">Al Tallah Road Bridge 1, 2 &amp; 3</h3>
              <div class="mt-2 flex justify-between items-end pt-2">
                <span class="text-[10px] opacity-70 text-white drop-shadow-sm">Ajman</span>
                <span class="material-symbols-outlined text-sm text-white drop-shadow-sm">arrow_forward</span>
              </div>
            </div>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/90 flex flex-col justify-center items-center text-white p-8 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Bridge Construction</span>
            <h3 class="text-2xl font-bold uppercase mb-3">Al Tallah Road Bridge 1, 2 &amp; 3</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[260px]">Comprehensive bridge construction works delivering improved connectivity in Ajman, UAE.</p>
            <a href="/projects/3-bridges-al-talah-road" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
        <div class="project-card relative group overflow-hidden h-[250px] md:h-[350px]" data-category="infrastructure all">
          <img alt="E20 Traffic Improvement and Bridge Construction" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/e20-traffic-bridges/photo_010.jpeg" />
          <div class="absolute inset-0 bg-black/10 group-hover:bg-black/30 transition-colors motion-base"></div>
          <div class="absolute bottom-4 left-4 z-20 text-white">
            <h3 class="text-lg font-bold uppercase mb-1 drop-shadow-md">Traffic Improvement E20</h3>
            <p class="text-[10px] uppercase tracking-wide opacity-90 drop-shadow-md">2 Bridges Construction</p>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Details</span>
            <h3 class="text-lg font-bold uppercase mb-3">E20 Traffic Improvement</h3>
            <div class="text-[11px] space-y-1 opacity-80">
              <p>Location: UAE</p>
              <p>Scope: Highway &amp; 2 Bridges</p>
            </div>
            <a href="/projects/traffic-improvement-e20" class="mt-4 text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
      </div>

      <!-- Column 2 - Company Logo & Projects List (Center) -->
      <div class="col-span-12 md:col-span-4 flex flex-col border-r border-black" data-category="infrastructure all">
        <div class="relative h-[600px] md:h-[800px] border-b md:border-b-0 border-black flex flex-col">
          <!-- Logo Section -->
          <div class="h-[280px] md:h-[300px] flex items-center justify-center p-8 md:p-10 bg-background">
            <img
              alt="ULTRACTION GENERAL CONTRACTING LLC Logo"
              src="/logo/UGC-LOGO-HQ.png"
              class="max-w-full max-h-[300px] object-contain"
            />
          </div>

          <!-- Projects List -->
          <div class="p-6 md:p-8 pt-4 md:pt-5 text-center flex-1">
            <h3 class="text-sm font-bold uppercase tracking-widest mb-4 border-b border-black/20 pb-3 text-primary text-center">Our Projects</h3>
            <div id="projects-list-carousel" class="projects-list-carousel relative overflow-hidden min-h-[250px]">
              <div id="projects-list-track" class="projects-list-track flex">
                {projectCarouselPages.map((projectPage, pageIndex) => (
                  <div
                    id={`projects-list-page-${pageIndex}`}
                    class="projects-list-slide w-full shrink-0"
                    data-project-page={pageIndex}
                    aria-hidden={pageIndex === 0 ? 'false' : 'true'}
                  >
                    <ul class="space-y-0.5">
                      {projectPage.map((project) => (
                        <li>
                          <a href={`/projects/${project.slug}`} class="projects-list-link flex items-center justify-between gap-3 group py-1.5 text-primary transition-colors motion-base">
                            <span class="project-link-label text-[11px] uppercase tracking-wide text-left">{project.title}</span>
                            <span class="material-symbols-outlined text-sm text-primary/70 group-hover:text-primary transition-all motion-base group-hover:translate-x-0.5">arrow_forward</span>
                          </a>
                        </li>
                      ))}
                    </ul>
                  </div>
                ))}
              </div>
            </div>
            <div class="mt-5 flex items-center justify-center gap-3">
              <button
                type="button"
                class="projects-list-nav inline-flex h-7 w-7 items-center justify-center rounded-full border border-primary/35 text-primary hover:bg-primary hover:text-white transition-colors motion-base"
                data-projects-prev
                aria-label="Show previous projects"
              >
                <span class="material-symbols-outlined text-base">chevron_left</span>
              </button>
              <div role="tablist" aria-label="Project list pages" class="flex items-center justify-center gap-3">
              {projectCarouselPages.map((_, pageIndex) => (
                <button
                  type="button"
                  role="tab"
                  aria-controls={`projects-list-page-${pageIndex}`}
                  aria-selected={pageIndex === 0 ? 'true' : 'false'}
                  class={`projects-list-dot w-2.5 h-2.5 rounded-full border border-primary transition-all motion-base ${
                    pageIndex === 0 ? 'bg-primary scale-110' : 'bg-transparent opacity-50 hover:opacity-100'
                  }`}
                  data-project-page-target={pageIndex}
                >
                  <span class="sr-only">Show projects page {pageIndex + 1}</span>
                </button>
              ))}
              </div>
              <button
                type="button"
                class="projects-list-nav inline-flex h-7 w-7 items-center justify-center rounded-full border border-primary/35 text-primary hover:bg-primary hover:text-white transition-colors motion-base"
                data-projects-next
                aria-label="Show next projects"
              >
                <span class="material-symbols-outlined text-base">chevron_right</span>
              </button>
            </div>
            <p id="projects-list-status" class="sr-only" aria-live="polite">
              Showing projects page 1 of {projectCarouselPages.length}.
            </p>
            <a href="/projects" class="mt-6 inline-block text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-primary hover:text-white transition-colors text-primary">
              View All Projects
            </a>
          </div>
        </div>
      </div>

      <!-- Column 3 -->
      <div class="col-span-12 md:col-span-4 flex flex-col" data-category="infrastructure all">
        <div class="project-card relative group overflow-hidden h-[250px] md:h-[350px] border-b border-black">
          <img alt="Inner Bypass Parallel to E45 Madinat Zayed" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/inner-bypass-e45/photo_014.jpeg" />
          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition-colors motion-base"></div>
          <div class="absolute top-4 right-4 z-20">
            <span class="material-symbols-outlined text-white opacity-80">arrow_outward</span>
          </div>
          <div class="absolute bottom-0 left-0 w-full p-6 text-white z-20 bg-gradient-to-t from-black/80 to-transparent">
            <h3 class="text-xl font-bold uppercase mb-1">Inner Bypass E45</h3>
            <p class="text-xs uppercase tracking-widest opacity-80">Selected Works 2020-2024</p>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <h3 class="text-xl font-bold uppercase mb-2">Inner Bypass E45</h3>
            <p class="text-[10px] uppercase tracking-widest mb-2 opacity-80">Madinat Zayed / Abu Dhabi</p>
            <span class="inline-block w-8 h-[1px] bg-white/40"></span>
            <p class="mt-3 text-xs opacity-90 max-w-[240px]">Strategic inner bypass road upgrade improving access and traffic flow across key corridors.</p>
            <a href="/projects/upgrading-inner-bypass-e45" class="mt-4 text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
        <div class="project-card relative group overflow-hidden h-[350px] md:h-[450px]" data-category="infrastructure all">
          <img alt="Utility Structure Design & Build" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/utility-structure/photo_005.jpeg" />
          <div class="absolute inset-0 bg-black/10 group-hover:bg-black/30 transition-colors motion-base"></div>
          <div class="absolute top-6 left-6 z-20">
            <div class="flex flex-col items-start gap-1">
              <span class="text-3xl font-bold text-white uppercase leading-none">04</span>
              <span class="h-[1px] w-8 bg-white/50"></span>
            </div>
          </div>
          <div class="absolute bottom-6 right-6 z-20 text-right">
            <h3 class="text-2xl font-bold text-white uppercase mb-1 leading-tight text-shadow-sm">Utility Structure<br/>Design & Build</h3>
            <p class="text-[10px] text-white uppercase tracking-widest font-medium">Stations 6+623 – 7+897</p>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/90 flex flex-col justify-center items-center text-white p-8 text-center z-30">
            <svg class="w-12 h-12 mb-4 text-white/40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1v5m-4 0h4"/>
            </svg>
            <h3 class="text-2xl font-bold uppercase mb-2">Utility Structures</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[200px]">Design & built of multiple utility structures across key stations.</p>
            <a href="/projects/utility-bridge" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
      </div>

      <!-- Additional Projects Row -->
      <div class="col-span-12 md:col-span-4 flex flex-col border-r border-t border-black" data-category="infrastructure all">
        <div class="project-card relative group overflow-hidden h-[300px] md:h-[400px] border-b border-black">
          <img alt="Infrastructure Road Works" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/inner-bypass-e45/photo_006.jpeg" />
          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition-colors motion-base"></div>
          <div class="absolute bottom-0 left-0 w-full p-6 text-white z-20 bg-gradient-to-t from-black/80 to-transparent">
            <span class="text-[10px] uppercase tracking-widest text-white">Public Infrastructure</span>
            <h3 class="text-xl font-bold uppercase mt-1">RAK Beach District</h3>
            <p class="text-[10px] uppercase tracking-wide opacity-90 mt-1">Phase 1 – Road Infrastructure</p>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Public Infrastructure</span>
            <h3 class="text-xl font-bold uppercase mb-3">RAK Beach District</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[240px]">Road and infrastructure works supporting the development of the RAK Beach District.</p>
            <a href="/projects" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
        <div class="project-card relative group overflow-hidden h-[300px] md:h-[400px]" data-category="infrastructure all">
          <img alt="E20 Bridge Construction" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/e20-traffic-bridges/photo_020.jpeg" />
          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition-colors motion-base"></div>
          <div class="absolute bottom-0 left-0 w-full p-6 text-white z-20 bg-gradient-to-t from-black/80 to-transparent">
            <span class="text-[10px] uppercase tracking-widest text-white">Bridge Engineering</span>
            <h3 class="text-xl font-bold uppercase mt-1">E20 Bridges Construction</h3>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Bridge Engineering</span>
            <h3 class="text-xl font-bold uppercase mb-3">E20 Bridges Construction</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[240px]">Construction of multiple bridges enhancing traffic capacity and safety along the E20 corridor.</p>
            <a href="/projects/traffic-improvement-e20" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
      </div>

      <div class="col-span-12 md:col-span-4 flex flex-col border-r border-t border-black" data-category="infrastructure all">
        <div class="project-card relative group overflow-hidden h-[300px] md:h-[400px] border-b border-black">
          <img alt="Naseem Al Bar Bridge Foundation Work" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/naseem-albar-bridge/photo_038.jpeg" />
          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition-colors motion-base"></div>
          <div class="absolute bottom-0 left-0 w-full p-6 text-white z-20 bg-gradient-to-t from-black/80 to-transparent">
            <span class="text-[10px] uppercase tracking-widest text-white">Design & Build</span>
            <h3 class="text-xl font-bold uppercase mt-1">Naseem Al Bar Bridge</h3>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Design &amp; Build</span>
            <h3 class="text-xl font-bold uppercase mb-3">Naseem Al Bar Bridge</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[240px]">Bridge project delivering durable connectivity and improved capacity for the surrounding district.</p>
            <a href="/projects/naseem-albar-bridge" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
        <div class="project-card relative group overflow-hidden h-[300px] md:h-[400px]" data-category="infrastructure all">
          <img alt="Al Tallah Road Bridge Ajman" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/al-tallah-bridge/photo_016.jpeg" />
          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition-colors motion-base"></div>
          <div class="absolute bottom-0 left-0 w-full p-6 text-white z-20 bg-gradient-to-t from-black/80 to-transparent">
            <span class="text-[10px] uppercase tracking-widest text-white">Infrastructure</span>
            <h3 class="text-xl font-bold uppercase mt-1">Al Tallah Bridge Ajman</h3>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Infrastructure</span>
            <h3 class="text-xl font-bold uppercase mb-3">Al Tallah Bridge Ajman</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[240px]">Key bridge infrastructure supporting regional transport routes in Ajman, UAE.</p>
            <a href="/projects/3-bridges-al-talah-road" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
      </div>

      <div class="col-span-12 md:col-span-4 flex flex-col border-t border-black" data-category="infrastructure all">
        <div class="project-card relative group overflow-hidden h-[300px] md:h-[400px] border-b border-black">
          <img alt="Inner Bypass E45 Madinat Zayed Road Work" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/inner-bypass-e45/photo_023.jpeg" />
          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition-colors motion-base"></div>
          <div class="absolute bottom-0 left-0 w-full p-6 text-white z-20 bg-gradient-to-t from-black/80 to-transparent">
            <span class="text-[10px] uppercase tracking-widest text-white">Road Upgrade</span>
            <h3 class="text-xl font-bold uppercase mt-1">Inner Bypass E45</h3>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Road Upgrade</span>
            <h3 class="text-xl font-bold uppercase mb-3">Inner Bypass E45</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[240px]">Upgrading critical sections of the Inner Bypass E45 to improve safety and capacity.</p>
            <a href="/projects/upgrading-inner-bypass-e45" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
        <div class="project-card relative group overflow-hidden h-[300px] md:h-[400px]" data-category="infrastructure all">
          <img alt="Utility Structure Construction" class="w-full h-full object-cover transition-transform motion-slow media-motion" src="/images/projects/utility-structure/photo_010.jpeg" />
          <div class="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition-colors motion-base"></div>
          <div class="absolute bottom-0 left-0 w-full p-6 text-white z-20 bg-gradient-to-t from-black/80 to-transparent">
            <span class="text-[10px] uppercase tracking-widest text-white">Utility Works</span>
            <h3 class="text-xl font-bold uppercase mt-1">Utility Structures</h3>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/95 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Utility Works</span>
            <h3 class="text-xl font-bold uppercase mb-3">Utility Structures</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[240px]">Specialized utility structures supporting critical services across multiple stations.</p>
            <a href="/projects/utility-bridge" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
      </div>

    </div>
  </section>

  <!-- Archive Section -->
  <SectionBackground client:visible showBottomLine={false} showTopBorder={false} showBottomBorder={true} showGrid={true} showAccentBlob={true}>
    <section aria-labelledby="archive-heading">
      <h2 id="archive-heading" class="sr-only">Project Archive</h2>
      <div class="grid grid-cols-1 md:grid-cols-4">
        <div class="col-span-1 md:col-span-1 border-r border-primary/20 p-8 flex flex-col justify-center aspect-square">
          <span class="text-[10px] font-bold uppercase tracking-widest opacity-80 mb-4 block">Archive</span>
          <h3 class="text-xl font-bold uppercase leading-tight mb-6">Explore Past<br/>Endeavors</h3>
          <a class="inline-flex items-center gap-2 text-[10px] uppercase tracking-widest font-bold hover:text-color-accent transition-colors motion-base" href="/contact">
            View All <span class="material-symbols-outlined text-sm">arrow_forward</span>
          </a>
        </div>
        <div class="col-span-1 md:col-span-1 border-r border-primary/20 relative group overflow-hidden aspect-square project-card">
          <img alt="E20 Traffic Bridges" class="w-full h-full object-cover transition-transform motion-slow media-motion group-hover:scale-110 grayscale" src="/images/projects/e20-traffic-bridges/photo_015.jpeg" />
          <div class="absolute bottom-4 left-4 z-10">
            <span class="text-[10px] font-bold uppercase bg-white/80 text-primary px-2 py-1">E20 Bridges</span>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/90 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Archive</span>
            <h3 class="text-sm font-bold uppercase mb-3">E20 Bridges</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[220px]">Archived visual from the E20 bridge works portfolio.</p>
            <a href="/projects/traffic-improvement-e20" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
        <div class="col-span-1 md:col-span-1 border-r border-primary/20 relative group overflow-hidden aspect-square project-card">
          <img alt="Inner Bypass E45" class="w-full h-full object-cover transition-transform motion-slow media-motion group-hover:scale-110 grayscale" src="/images/projects/inner-bypass-e45/photo_025.jpeg" />
          <div class="absolute bottom-4 left-4 z-10">
            <span class="text-[10px] font-bold uppercase bg-white/80 text-primary px-2 py-1">Inner Bypass E45</span>
          </div>
          <div class="project-overlay absolute inset-0 bg-primary/90 flex flex-col justify-center items-center text-white p-6 text-center z-30">
            <span class="text-[10px] uppercase tracking-widest text-white mb-2">Archive</span>
            <h3 class="text-sm font-bold uppercase mb-3">Inner Bypass E45</h3>
            <p class="text-xs opacity-90 mb-4 max-w-[220px]">Selected archival imagery from the Inner Bypass E45 project.</p>
            <a href="/projects/upgrading-inner-bypass-e45" class="text-[10px] uppercase tracking-widest font-bold border border-black/30 px-4 py-2 hover:bg-white hover:text-primary transition-colors cursor-pointer">
              Explore
            </a>
          </div>
        </div>
        <div class="col-span-1 md:col-span-1 relative group overflow-hidden aspect-square p-8 flex flex-col justify-between">
          <div>
            <span class="material-symbols-outlined text-3xl mb-2">folder_open</span>
            <p class="text-sm font-medium leading-relaxed opacity-90">Access our complete database of over 150 projects.</p>
          </div>
          <div class="text-[10px] uppercase tracking-widest font-bold opacity-70">Database Access</div>
        </div>
      </div>
    </section>
  </SectionBackground>

  <!-- Final CTA Section - Matching About Page Style -->
  <section class="relative bg-surface text-primary overflow-hidden border-y border-primary/20">
    <div class="absolute inset-0 pointer-events-none">
      <div
        class="absolute inset-0 opacity-[0.03]"
        style={{
          backgroundImage: `
            linear-gradient(to right, currentColor 1px, transparent 1px),
            linear-gradient(to bottom, currentColor 1px, transparent 1px)
          `,
          backgroundSize: '60px 60px'
        }}
      />
      <div class="absolute top-0 left-1/4 w-96 h-96 bg-accent/5 rounded-full blur-3xl animate-pulse-slow" />
    </div>

    <div class="relative z-10 px-6 md:px-12 lg:px-24 py-20 md:py-28 lg:py-32">
      <div class="flex flex-col items-center justify-center text-center max-w-4xl mx-auto">
        <div class="flex items-center gap-3 mb-6">
          <div class="w-12 h-[2px] bg-accent"></div>
          <span class="text-xs font-bold uppercase tracking-[0.3em] text-accent">
            Start Your Project
          </span>
          <div class="w-12 h-[2px] bg-accent"></div>
        </div>

        <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold font-heading leading-[0.95] tracking-tight mb-8 max-w-3xl">
          Let's build infrastructure that
          <br />
          <span class="italic font-light text-primary/50 scale-y-75 inline-block">
            stands the test of time.
          </span>
        </h2>

        <a
          class="group relative inline-flex items-center gap-3 px-10 py-5 bg-primary text-white overflow-hidden transition-all duration-300 hover:bg-accent hover:shadow-xl hover:shadow-accent/20"
          href="/contact"
        >
          <span class="relative z-10 text-xs font-bold uppercase tracking-[0.2em]">
            Discuss Your Project
          </span>
          <svg
            class="relative z-10 w-4 h-4 transition-transform duration-300 group-hover:translate-x-1"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              d="M14 5l7 7m0 0l-7 7m7-7H3"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth={2}
            />
          </svg>
        </a>
      </div>
    </div>

    <div class="absolute bottom-0 left-0 w-full h-[2px] bg-gradient-to-r from-transparent via-accent to-transparent"></div>
  </section>
</Layout>

<script>
  // Carousel/Filter functionality
  const filterBtns = document.querySelectorAll('.filter-btn');
  const filterTargets = document.querySelectorAll('[data-category]');
  const filterStatus = document.getElementById('projects-filter-status');

  const formatFilterLabel = (filter) => {
    if (!filter || filter === 'all') return 'all';
    return filter.replace('-', ' ');
  };

  filterBtns.forEach((btn) => {
    if (!(btn instanceof HTMLElement)) return;
    btn.addEventListener('click', () => {
      const filter = btn.dataset.filter;
      if (!filter) return;

      // Update active states on buttons
      filterBtns.forEach((b) => {
        if (!(b instanceof HTMLElement)) return;
        b.classList.remove('active', 'bg-primary', 'text-white');
        b.classList.add('bg-background', 'dark:bg-card', 'text-black');
        b.setAttribute('aria-pressed', 'false');
      });

      // Set clicked button as active
      btn.classList.add('active', 'bg-primary', 'text-white');
      btn.classList.remove('bg-background', 'dark:bg-card', 'text-black');
      btn.setAttribute('aria-pressed', 'true');

      let visibleCount = 0;

      // Filter all categorized containers/cards
      filterTargets.forEach((target) => {
        if (!(target instanceof HTMLElement)) return;
        const categories = (target.dataset.category ?? '').split(/\s+/).filter(Boolean);
        const isMatch = filter === 'all' || categories.includes(filter);

        if (isMatch) {
          target.hidden = false;
          target.removeAttribute('aria-hidden');
          visibleCount += 1;
        } else {
          target.hidden = true;
          target.setAttribute('aria-hidden', 'true');
        }
      });

      if (filterStatus) {
        filterStatus.textContent = `Showing ${formatFilterLabel(filter)} projects (${visibleCount} sections visible).`;
      }
    });
  });

  // Center list smooth carousel
  const projectListCarousel = document.getElementById('projects-list-carousel');
  const projectListTrack = document.getElementById('projects-list-track');
  const projectListPages = document.querySelectorAll('.projects-list-slide');
  const projectListDots = document.querySelectorAll('.projects-list-dot');
  const projectListPrev = document.querySelector('[data-projects-prev]');
  const projectListNext = document.querySelector('[data-projects-next]');
  const projectListStatus = document.getElementById('projects-list-status');
  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  let projectListActiveIndex = 0;
  let projectListTimer = 0;

  const setProjectListPage = (targetIndex, announce = true) => {
    if (!(projectListTrack instanceof HTMLElement) || projectListPages.length === 0) return;
    const pageCount = projectListPages.length;
    const nextIndex = ((targetIndex % pageCount) + pageCount) % pageCount;
    projectListActiveIndex = nextIndex;
    projectListTrack.style.transform = `translateX(-${nextIndex * 100}%)`;

    projectListPages.forEach((page, pageIndex) => {
      if (!(page instanceof HTMLElement)) return;
      const isActive = pageIndex === nextIndex;
      page.setAttribute('aria-hidden', String(!isActive));
    });

    projectListDots.forEach((dot, dotIndex) => {
      if (!(dot instanceof HTMLElement)) return;
      const isActive = dotIndex === nextIndex;
      dot.setAttribute('aria-selected', String(isActive));
      dot.classList.toggle('bg-primary', isActive);
      dot.classList.toggle('scale-110', isActive);
      dot.classList.toggle('bg-transparent', !isActive);
      dot.classList.toggle('opacity-50', !isActive);
      dot.classList.toggle('hover:opacity-100', !isActive);
    });

    if (projectListStatus && announce) {
      projectListStatus.textContent = `Showing projects page ${nextIndex + 1} of ${projectListPages.length}.`;
    }
  };

  const stopProjectListAutoplay = () => {
    if (projectListTimer) {
      window.clearInterval(projectListTimer);
      projectListTimer = 0;
    }
  };

  const startProjectListAutoplay = () => {
    if (prefersReducedMotion || projectListPages.length <= 1) return;
    stopProjectListAutoplay();
    projectListTimer = window.setInterval(() => {
      setProjectListPage(projectListActiveIndex + 1, false);
    }, 4500);
  };

  projectListDots.forEach((dot) => {
    if (!(dot instanceof HTMLElement)) return;
    dot.addEventListener('click', () => {
      const targetIndex = Number(dot.dataset.projectPageTarget ?? 0);
      if (Number.isNaN(targetIndex)) return;
      setProjectListPage(targetIndex);
      startProjectListAutoplay();
    });
  });

  if (projectListPrev instanceof HTMLElement) {
    projectListPrev.addEventListener('click', () => {
      setProjectListPage(projectListActiveIndex - 1);
      startProjectListAutoplay();
    });
  }

  if (projectListNext instanceof HTMLElement) {
    projectListNext.addEventListener('click', () => {
      setProjectListPage(projectListActiveIndex + 1);
      startProjectListAutoplay();
    });
  }

  if (projectListCarousel instanceof HTMLElement) {
    projectListCarousel.addEventListener('mouseenter', stopProjectListAutoplay);
    projectListCarousel.addEventListener('mouseleave', startProjectListAutoplay);
    projectListCarousel.addEventListener('focusin', stopProjectListAutoplay);
    projectListCarousel.addEventListener('focusout', startProjectListAutoplay);
  }

  setProjectListPage(0, false);
  startProjectListAutoplay();
</script>

<style>
  .projects-list-track {
    transition: transform 600ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .projects-list-link {
    color: rgb(20, 20, 20);
  }

  .projects-list-link .project-link-label {
    position: relative;
    display: inline-block;
  }

  .projects-list-link .project-link-label::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -3px;
    width: 100%;
    height: 1px;
    background: currentColor;
    transform: scaleX(0);
    transform-origin: left center;
    transition: transform 320ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .projects-list-link:hover .project-link-label::after,
  .projects-list-link:focus-visible .project-link-label::after {
    transform: scaleX(1);
  }

  /* Animation for CTA section */
  @keyframes pulse-slow {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.1); }
  }

  .animate-pulse-slow {
    animation: pulse-slow 8s ease-in-out infinite;
  }
</style>

