---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Careers - ULTRACTION GENERAL CONTRACTING LLC">
  <section class="careers-hero">
    <div class="careers-card">
      <p class="eyebrow">Careers</p>
      <h1>Join Our Team</h1>
      <p class="lead">
        ULTRACTION is always looking for driven engineers, site professionals, and project leaders who want to build high-impact infrastructure.
      </p>
      <p class="body">
        If you are ready to contribute to complex construction and contracting projects across the UAE and KSA, send us your profile and tell us where you can add value.
      </p>
      <a class="cta" href="/contact?subject=careers">Apply via Contact Form</a>
      <p class="note">Include your role, years of experience, availability, and a link to your CV or portfolio in the message.</p>
    </div>
  </section>
</Layout>

<style>
  .careers-hero {
    min-height: calc(100vh - 104px);
    display: grid;
    place-items: center;
    padding: 3rem 1.5rem;
    background:
      radial-gradient(circle at 20% 20%, rgba(221, 212, 197, 0.7) 0%, rgba(221, 212, 197, 0) 45%),
      linear-gradient(135deg, #f7f2e8 0%, #e9e0d1 42%, #d8cfbf 100%);
  }

  .careers-card {
    width: min(760px, 100%);
    padding: clamp(2rem, 4vw, 3.75rem);
    border: 1px solid rgba(35, 35, 35, 0.2);
    background: rgba(255, 255, 255, 0.72);
    backdrop-filter: blur(4px);
    box-shadow: 0 24px 50px rgba(35, 35, 35, 0.12);
  }

  .eyebrow {
    margin: 0 0 1rem;
    text-transform: uppercase;
    letter-spacing: 0.22em;
    font-size: 0.72rem;
    font-weight: 700;
    color: #5f5346;
  }

  h1 {
    margin: 0;
    color: #1d1d1d;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 0.95;
    text-transform: uppercase;
    font-family: var(--font-heading);
  }

  .lead {
    margin: 1.5rem 0 0;
    color: #2a2a2a;
    font-size: clamp(1rem, 2vw, 1.25rem);
    line-height: 1.65;
    max-width: 62ch;
  }

  .body {
    margin: 1rem 0 0;
    color: rgba(29, 29, 29, 0.84);
    line-height: 1.75;
    max-width: 62ch;
  }

  .cta {
    margin-top: 2rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.95rem 1.4rem;
    background: #1d1d1d;
    color: #fff;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-size: 0.78rem;
    font-weight: 700;
    transition: transform 0.2s ease, background-color 0.2s ease;
  }

  .cta:hover {
    background: #000;
    transform: translateY(-2px);
  }

  .note {
    margin-top: 1rem;
    color: rgba(29, 29, 29, 0.72);
    font-size: 0.9rem;
    line-height: 1.6;
  }
</style>
